import { ReactGraphicsEditor } from './react-graphics-editor';

// Test file for ReactGraphicsEditor
describe('ReactGraphicsEditor Basic Tests', () => {
  it('should run basic test', () => {
    expect(true).toBe(true);
  });

  it('should import component', () => {
    expect(ReactGraphicsEditor).toBeDefined();
  });
});
