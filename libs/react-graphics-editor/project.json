{
  "name": "react-graphics-editor",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/react-graphics-editor/src",
  "projectType": "library",
  "tags": ["scope:shared", "type:feature"],
  "// targets": "to see all targets run: nx show project react-graphics-editor --web",
  "targets": {
    "build": {
      "executor": "@nx/vite:build",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "config": "libs/react-graphics-editor/vite.config.ts"
      }
    },
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "config": "libs/react-graphics-editor/vite.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "storybook": {
      "executor": "@nx/storybook:storybook",
      "options": {
        "configDir": "libs/react-graphics-editor/.storybook",
        "port": 6009
      }
    },
    "build-storybook": {
      "executor": "@nx/storybook:build",
      "outputs": ["{workspaceRoot}/dist/storybook/libs/react-graphics-editor"],
      "options": {
        "configDir": "libs/react-graphics-editor/.storybook",
        "outputDir": "dist/storybook/libs/react-graphics-editor"
      }
    }
  }
}
