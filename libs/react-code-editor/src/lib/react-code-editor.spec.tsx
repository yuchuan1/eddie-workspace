import ReactCodeEditor from './react-code-editor';

describe('ReactCodeEditor', () => {
  it('should be defined', () => {
    expect(ReactCodeEditor).toBeDefined();
  });

  it('should be a function', () => {
    expect(typeof ReactCodeEditor).toBe('function');
  });
});
